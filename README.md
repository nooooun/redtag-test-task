# redtag-test-task

1.  In the root directory (where `docker-compose.yml` is located), run the following command:

    ```bash
    docker-compose up --build
    ```

2.  Once all services are running, open your browser and go to:

    ```http://localhost:5173```